# AI Follow-Up Starter Kit

Brian's owner-only lead magnet is a seven-page original PDF at `output/pdf/AI-Follow-Up-Starter-Kit.pdf`. It contains an input worksheet, five prompts, a fictional worked example, a review checklist, and links to the free Summit and Shop. It makes no customer-result or income claims. The material is readable without paid software; the examples can be used with an AI tool the reader already has access to.

The PDF was rendered and visually inspected on all seven pages. Text extraction verifies readable text on every page and three working URL annotations on the last page. File size: **103,372 bytes**. SHA-256: `db76186cd892ee5e4d9c6f6888bfac9c78fccf28782a1ce230795f7d0e0e47ee`.

## Publication

Upload the exact file, without overwrite, to private bucket `offer-files` at `e605661a-8652-4949-b20c-8ec37c4f98d6/2026-09-19-v1-ai-follow-up-starter-kit.pdf`. Verify stored bytes against the hash. The guarded owner-content transaction `scripts/maintenance/20260919-ai-follow-up-starter-kit.sql` creates the real published, listed free offer only when the owner identity and private file metadata match. Never run it on a member installation. It deliberately refuses to overwrite an existing offer.

The published landing-page path is `/offers/ai-follow-up-starter-kit`. Free native claims use the existing private order/download mechanism; the transactional email implementation provides delivery and recovery. Claiming does not enroll someone in the newsletter. The Start Here page finds a free resource from the actual public featured catalog, so it does not advertise an unpublished kit or hardcode Brian's offer into member sites.

## Maintenance

Edit the landing-page copy in Admin → Offers & shop. To replace the PDF, upload a new file through that editor; previous customers retain their original file snapshot. The kit grants use of the prompts and worksheets in the reader's own work and client work, with no resale or redistribution of the original kit. A member should create their own resource and contact details.

The worksheet's human-review approach is consistent with the [NIST Generative AI Profile](https://nvlpubs.nist.gov/nistpubs/ai/NIST.AI.600-1.pdf). The content is original practical guidance and is not a NIST certification or endorsement.
